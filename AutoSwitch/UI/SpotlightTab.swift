// Launcher rules are now managed inside AppRulesTab. This file is kept as a
// stub to satisfy the Xcode project file list; re-run generate_project.rb to
// remove it cleanly.

import SwiftUI

struct SpotlightTab: View {
    var body: some View { EmptyView() }
}
